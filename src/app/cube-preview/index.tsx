//import React from "react";
import { Scramble } from "app/common/cube/scramble";

interface CubePreviewProps {
  scramble: Scramble;
}

export function CubePreview(props: CubePreviewProps): JSX.Element | null {
  return null;
}
